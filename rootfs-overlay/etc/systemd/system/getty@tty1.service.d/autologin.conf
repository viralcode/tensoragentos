# AInux Auto-Login Configuration
# This goes into /etc/systemd/system/getty@tty1.service.d/autologin.conf
# systemd drops in that auto-logs in the ainux user on tty1

[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin ainux --noclear %I $TERM
